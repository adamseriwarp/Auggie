# Google OAuth credentials — never commit these
credentials.json
token.json

# Pipeline outputs — regenerated by running the script
output/

# Python
__pycache__/
*.py[cod]
*.pyo

# uv / virtualenv
.venv/
.python-version

